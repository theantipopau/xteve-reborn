# Developer notes

See **[`CLAUDE.md`](CLAUDE.md)** for build/test commands, the generated-artifact rules
(regenerating `src/webUI.go` after any `html/` change; `html/js/*_ts.js` being compiled
from `ts/`), and the project's conventions.

See **[`runningchangelog.md`](runningchangelog.md)** for the running log of changes —
what changed, why, and how each one was verified, including what is explicitly *not*
verified yet.

Release notes for the current version live in `README.md`; `changelog-beta.md` is
upstream's frozen 2.x history and should not be appended to.
